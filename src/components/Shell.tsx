import{Bell,CalendarDays,Compass,Plus,UserRound}from'lucide-react';import{Link,NavLink,Outlet}from'react-router-dom';
const nav=[['/discover','Discover',Compass],['/create','Create',Plus],['/activities','My activities',CalendarDays],['/notifications','Notifications',Bell],['/profile','Profile',UserRound]] as const;
export function Shell(){return <><header className="topbar"><Link className="brand" to="/"><span className="brandmark">P</span>PlayNest</Link><span className="safety-note">Guardian-attended activities</span></header><div className="app-layout"><aside className="side-nav" aria-label="Main navigation">{nav.map(([to,label,Icon])=><NavLink key={to} to={to}><Icon size={20}/>{label}</NavLink>)}</aside><main className="page"><Outlet/></main></div><nav className="bottom-nav" aria-label="Main navigation">{nav.map(([to,label,Icon])=><NavLink key={to} to={to}><Icon/><span>{label}</span></NavLink>)}</nav></>}
