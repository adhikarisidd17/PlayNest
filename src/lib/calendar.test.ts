import{describe,expect,it}from'vitest';import{eventIcs}from'./calendar';import{examples}from'../data/demo';describe('calendar privacy',()=>{it('contains event basics but no child profile information',()=>{const text=eventIcs(examples[0]!);expect(text).toContain('Guardian attendance required');expect(text).toContain('DTSTART:');expect(text).not.toMatch(/nickname|participation.notes|attendee|surname/i)})});
