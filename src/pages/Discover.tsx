import{Search,SlidersHorizontal}from'lucide-react';import{useEffect,useMemo,useState}from'react';import{EventCard}from'../components/EventCard';import{localities}from'../data/demo';import{supabase}from'../lib/supabase';import type{PlayEvent}from'../types';
type EventRow=PlayEvent&{venue:PlayEvent['venue']|null;locality:PlayEvent['locality']|null};
export function Discover(){const[q,setQ]=useState('');const[locality,setLocality]=useState('all');const[events,setEvents]=useState<PlayEvent[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');useEffect(()=>{let active=true;async function load(){setLoading(true);const{data,error}=await supabase.from('events').select('*,venue:venues(id,name,address_text,directions_url),locality:localities(id,name,city,country_code,timezone)').eq('is_example',false).in('status',['published','completed']).order('starts_at',{ascending:true});if(!active)return;if(error)setError(error.message);else setEvents(((data??[])as EventRow[]).map(e=>({...e,venue:e.venue??undefined,locality:e.locality??undefined})));setLoading(false)}void load();return()=>{active=false}},[]);const filtered=useMemo(()=>events.filter(e=>(locality==='all'||e.locality_id===locality)&&e.title.toLowerCase().includes(q.toLowerCase())),[events,q,locality]);const now=Date.now();const upcoming=filtered.filter(e=>e.status==='published'&&new Date(e.ends_at).getTime()>=now);const past=filtered.filter(e=>e.status==='completed'||new Date(e.ends_at).getTime()<now);return <><div className="page-title"><span className="eyebrow">Discover</span><h1>What’s happening nearby?</h1><p>Browse real activities published by local organizers.</p></div><div className="filters"><label className="search"><Search/><span className="sr-only">Search events</span><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search activities"/></label><label><span className="sr-only">Locality</span><select value={locality} onChange={e=>setLocality(e.target.value)}><option value="all">All localities</option>{localities.map(l=><option value={l.id} key={l.id}>{l.name}</option>)}</select></label><button className="icon-button" aria-label="More filters"><SlidersHorizontal/></button></div>{error&&<p className="notice" role="alert">Could not load events: {error}</p>}{loading?<div className="center"><div className="spinner"/>Loading events…</div>:<>{upcoming.length?<><div className="section-head"><h2>Upcoming events</h2><span>{upcoming.length} available</span></div><div className="card-grid">{upcoming.map(e=><EventCard event={e} key={e.id}/>)}</div></>:<section className="empty-callout"><h2>Playdates are starting in this area.</h2><p>No upcoming real events match this view. Host the first event or invite another parent.</p><div className="actions"><a className="button small" href="/create">Host the first event</a></div></section>}{past.length>0&&<><div className="section-head past-heading"><h2>Past events</h2><span>Completed activities</span></div><div className="card-grid">{past.map(e=><EventCard event={e} key={e.id}/>)}</div></>}</>}</>}
