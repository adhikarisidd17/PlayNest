import{Calendar,ChevronLeft,Clock,ExternalLink,Flag,Languages,MapPin,Share2,ShieldCheck,Users}from'lucide-react';import{format}from'date-fns';import{Link,useParams}from'react-router-dom';import{useEffect,useState}from'react';import{examples}from'../data/demo';import{supabase}from'../lib/supabase';import{downloadIcs,googleCalendarUrl}from'../lib/calendar';import type{PlayEvent}from'../types';
type EventRow=PlayEvent&{venue:PlayEvent['venue']|null;locality:PlayEvent['locality']|null};
export function EventDetail(){const{id}=useParams();const[event,setEvent]=useState<PlayEvent|undefined>(()=>examples.find(x=>x.id===id));const[loading,setLoading]=useState(!event);const[error,setError]=useState('');useEffect(()=>{if(!id||event)return;let active=true;async function load(){const{data,error}=await supabase.from('events').select('*,venue:venues(id,name,address_text,directions_url),locality:localities(id,name,city,country_code,timezone)').eq('id',id).single();if(!active)return;if(error)setError(error.message);else{const row=data as EventRow;setEvent({...row,venue:row.venue??undefined,locality:row.locality??undefined})}setLoading(false)}void load();return()=>{active=false}},[id,event]);if(loading)return <div className="center"><div className="spinner"/>Loading event…</div>;if(!event)return <div className="empty"><h1>Event not found</h1><p>{error}</p><Link to="/discover">Back to discover</Link></div>;const e=event;const isPast=e.status==='completed'||new Date(e.ends_at).getTime()<Date.now();const share=async()=>{const data={title:e.title,text:'See this guardian-attended activity on PlayNest',url:location.href};if(navigator.share)await navigator.share(data);else await navigator.clipboard.writeText(location.href)};return <article className="detail"><Link className="back" to="/discover"><ChevronLeft/> Back to discover</Link><div className="detail-head"><div><span className="eyebrow">{e.is_example?'Completed demonstration':isPast?'Past event':e.category}</span><h1>{e.title}</h1><p>{e.description}</p></div><div className="detail-actions"><button className="icon-button" onClick={share} aria-label="Share event"><Share2/></button></div></div><div className="detail-grid"><div><section className="info-card"><h2>When & where</h2><p><Calendar/> <span><b>{format(new Date(e.starts_at),'EEEE d MMMM yyyy')}</b><br/>{format(new Date(e.starts_at),'HH:mm')}–{format(new Date(e.ends_at),'HH:mm')} ({e.timezone})</span></p><p><MapPin/><span><b>{e.venue?.name}</b><br/>{e.venue?.address_text}</span></p><div className="actions"><a className="button secondary small" href={e.venue?.directions_url??`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(`${e.venue?.name} ${e.venue?.address_text}`)}`} target="_blank" rel="noreferrer">Directions <ExternalLink/></a><button className="button secondary small" onClick={()=>downloadIcs(e)}>Download .ics</button><a className="button secondary small" href={googleCalendarUrl(e)} target="_blank" rel="noreferrer">Google Calendar</a></div></section><section className="info-card"><h2>About this activity</h2><div className="fact-grid"><p><Users/><span><small>Suitable ages</small>{e.age_bands.join(', ')}</span></p><p><ShieldCheck/><span><small>Supervision</small>Guardian required</span></p><p><Languages/><span><small>Languages</small>{e.languages.join(', ')}</span></p><p><Clock/><span><small>Setting</small>{e.environment}</span></p></div>{e.accessibility_notes&&<p><b>Accessibility:</b> {e.accessibility_notes}</p>}</section><button className="text-button danger"><Flag/> Report this event</button></div><aside className="join-card"><ShieldCheck/><h2>{e.is_example?'Completed demonstration':isPast?'This event has ended':'Request a place'}</h2><p>{e.is_example?'This demonstration is clearly labelled and cannot be joined.':isPast?'Past events remain visible as history but no longer accept requests.':'Sign in and choose the child profiles you want to share privately with the organizer.'}</p><button className="button" disabled={e.is_example||isPast}>{e.is_example||isPast?'Joining unavailable':'Request a place'}</button><small>Guardians must attend and remain responsible for their children.</small></aside></div></article>}
