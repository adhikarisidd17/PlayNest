import{useState}from'react';const tabs=['Upcoming','Pending','Waitlisted','Place offers','Hosting','Past','Cancelled'];export function Dashboard(){const[tab,setTab]=useState('Upcoming');return <><div className="page-title"><span className="eyebrow">My activities</span><h1>Your plans in one place</h1></div><div className="tabs">{tabs.map(x=><button className={tab===x?'active':''} onClick={()=>setTab(x)} key={x}>{x}</button>)}</div><div className="empty"><h2>No {tab.toLowerCase()} activities</h2><p>When you request or host a real activity, it will appear here.</p><a className="button small" href="/discover">Discover activities</a></div></>}
