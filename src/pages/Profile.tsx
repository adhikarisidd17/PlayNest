import{supabase}from'../lib/supabase';export function Profile(){return <><div className="page-title"><span className="eyebrow">Profile</span><h1>Your family settings</h1></div><section className="info-card"><h2>Privacy by default</h2><p>Child profiles use only a nickname, broad age band and interests. They are never publicly searchable.</p><button className="button secondary small">Add child profile</button></section><section className="info-card"><h2>Account</h2><div className="actions"><button className="button secondary small" onClick={()=>supabase.auth.signOut()}>Sign out</button><button className="text-button danger" onClick={()=>alert('Account deletion requires the delete-account Edge Function. Confirm there to permanently remove or anonymize your data.')}>Delete account</button></div></section></>}
